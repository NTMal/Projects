# ── Requirements for CC Fraud Detection API ──────────────────────────────────
#
# These are the Python packages needed to run the FastAPI app inside Docker.
# Versions are pinned (==) to ensure the container behaves identically
# on any machine — your laptop, CI/CD, or GCP Cloud Run.
#
# To update a version: change the number and rebuild the Docker image.

# Web framework — handles HTTP requests, routing, and auto-generates /docs
fastapi==0.111.0

# ASGI server — runs the FastAPI app inside the container
# uvicorn is the standard production server for FastAPI
uvicorn==0.29.0

# Data manipulation — used in preprocessing (DataFrame creation, column ops)
pandas==3.0.2

# Numerical computing — used for array operations in preprocessing
numpy==2.4.4

# Machine learning — Random Forest, Isolation Forest, StandardScaler
scikit-learn==1.8.0

# Model serialisation — saves/loads .joblib model files efficiently
# (comes with scikit-learn but listed explicitly for clarity)
joblib==1.5.3
